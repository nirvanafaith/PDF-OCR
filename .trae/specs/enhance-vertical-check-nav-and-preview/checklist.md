# Checklist

- [x] 字符列表上方"返回上一步"按钮已删除,原位放置字符跳转输入框 + 确认按钮
- [x] 切片展示区底部左侧有"上一步"按钮,第一字符时显示"返回导入"并点击返回导入阶段
- [x] 非第一字符时"上一步"按钮显示"上一步",点击先 flush 再切换到上一字符
- [x] 最后一个字符时"下一步"按钮文字显示"进入横校"
- [x] 非最后一字符时"下一步"按钮文字显示"下一步"
- [x] 字符跳转输入框输入存在字符并确认/回车后,切换到对应字符集合
- [x] 字符跳转输入框输入不存在字符并确认/回车后,弹出 QMessageBox 提示未找到
- [x] 单选切片时按上/下/左/右方向键,选中切换到当前页对应方向的相邻切片
- [x] 方向键导航到头后再按同方向键,循环到另一侧
- [x] Enter/Return 键不再触发"下一步"(Space 仍可触发)
- [x] 多选或未选切片时按方向键不触发导航
- [x] 原图预览显示选中切片所属 PDF 整行的全宽长条(从左边界到右边界)
- [x] 整行长条预览中红框正确标出当前字符位置并居中
- [x] `python -m py_compile ui\vertical_check_window.py` 通过
- [x] 无残留 `back_btn` 引用与 `_on_back` 方法定义
- [x] QMessageBox.warning、QLineEdit.returnPressed、Qt.Key_* API 使用符合 PyQt5 文档
