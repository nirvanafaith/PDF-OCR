# 软件2 C++ 重构验收检查清单

## 阶段 1：工程骨架与基础设施

- [ ] `CMakeLists.txt` 可正常 configure 并生成 Visual Studio 或 Ninja 工程
- [ ] 项目能成功编译出可执行文件
- [ ] Qt6 Widgets/Gui/Core 已正确链接
- [ ] nlohmann/json 已可用
- [ ] PDF 渲染与生成库已可用
- [ ] 全局 QSS 样式表文件已创建并能在应用启动时加载
- [ ] `zoom_utils` 与 `JsonUtils` 已移植并可通过单元测试

## 阶段 2：数据模型与核心处理逻辑

- [ ] `datamodels.h` 中所有 Python 对应数据结构已完整定义
- [ ] `flatten_bbox` 函数行为与 Python 版本一致
- [ ] 所有模型支持从 nlohmann/json 读取并写回 JSON
- [ ] `PDFProcessor::convert_to_images` 能将 PDF 渲染为 `QImage` 列表（200 DPI）
- [ ] `PDFOutputGenerator::generate` 能生成红色文字版与透明文字版 PDF
- [ ] 生成 PDF 中字符位置、字体大小、颜色与 Python 版本一致
- [ ] `OCREngine::load_results_from_file` 能正确读取 lines.json 与 chars.json
- [ ] `OCREngine::parse_and_group` 分组结果与 Python 版本一致
- [ ] `OCREngine::build_line_data` 构建的 LineSlice 与 Python 版本一致

## 阶段 3：UI 窗口移植

### 主窗口

- [ ] 主窗口标题为"横校工具"，最小尺寸 1200×800
- [ ] 顶部步骤指示器显示 导入/纵校/横校/精修，并随阶段切换高亮
- [ ] 各阶段窗口可通过信号槽切换

### 导入窗口

- [ ] 界面布局与 Python 版本一致（PDF、lines.json、字符JSON 三行选择）
- [ ] 选择 PDF 后自动检测同目录 JSON 文件
- [ ] "开始加载"按钮在 PDF 选择后可用
- [ ] 加载过程在后台线程执行，UI 不卡顿
- [ ] 进度日志实时显示
- [ ] 加载完成后自动进入纵校阶段

### 纵校窗口

- [ ] 左侧字符列表按字符文本分组
- [ ] 右侧切片网格每行 8 列，每个切片 90×90
- [ ] 低分切片显示浅黄色底色与橙黄色边框
- [ ] 点击切片后顶部预览区显示整页横条，红框居中
- [ ] 预览区支持无边界拖拽和 Ctrl+滚轮缩放
- [ ] 点击切片可修改字符文本
- [ ] 右键切片可删除
- [ ] 修改/删除后仍停留在当前字符组
- [ ] "下一步"进入横校阶段，"返回"回到导入阶段

### 横校窗口

- [ ] 双视图并排：左为叠加文字页，右为原图页
- [ ] 工具栏包含返回、上一页、页码、页码输入框、下一页、手型工具、放大、缩放百分比、缩小、适合宽度、适合高度
- [ ] 字符按 bbox 渲染，字体大小自适应 bbox 宽度
- [ ] 忽略行以灰色显示
- [ ] 鼠标悬停显示行切片图像提示
- [ ] 右键菜单支持修改文本和忽略/取消忽略
- [ ] 左右视图滚动条联动
- [ ] "完成横校"进入精修阶段，"返回"回到纵校阶段

### 精修窗口

- [ ] 每页文字项以红色矩形显示
- [ ] 工具栏包含返回、上一页、页码、下一页、手型工具、拖拽工具、添加文字工具、删除、缩放
- [ ] 文字项可拖拽移动、缩放手柄调整大小、双击编辑、右键删除
- [ ] "导出 PDF"在后台线程生成两份 PDF，并显示进度对话框
- [ ] "完成"结束流程并返回导入阶段

## 阶段 4：集成、测试与优化

- [ ] 使用真实数据跑通完整流程：导入 → 纵校 → 横校 → 精修 → 导出 PDF
- [ ] C++ 版本输出 PDF 与 Python 版本输出 PDF 视觉一致
- [ ] 大文档（50 页以上）加载和翻页无明显卡顿
- [ ] 常见异常（文件不存在、JSON 格式错误、PDF 损坏）有明确提示
- [ ] `README.md` 包含完整构建与运行说明
- [ ] Python 版本代码未被破坏或删除
- [ ] 在 Windows 7 SP1 64 位环境中可启动并显示导入界面
